\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath}

\title{Paper 1 (SAR-CRP v2 Core) --- Full Q1 Experiment Suite Report}
\author{}
\date{\today}

\begin{document}
\maketitle

\section{Setup}

\textbf{Hardware.} All computation ran on the remote CPU server
(\texttt{a100-B}, \texttt{story\_research} conda env); the laptop was used
only to edit code (spec \S51's disclosure requirement). CPU: Intel(R)
Xeon(R) Gold 6226R @ 2.90GHz, 2 sockets $\times$ 16 cores $\times$ 2
threads/core = 64 logical CPUs, 2 NUMA nodes. Software: Python 3.11.15,
PyTorch 2.13.0+cpu (CPU-only build --- CRP\_RL inference in this report ran
on CPU, not GPU), SciPy 1.17.1, NumPy 2.4.6.

\textbf{Seed policy.} \texttt{sarcrp.seed\_policy} distinguishes
\texttt{DEV\_SEEDS} (0--9, inspected by hand while diagnosing earlier bugs
--- e.g.\ the MVP report's seed-7 trace) from \texttt{REPORT\_SEEDS}
(20--39, fresh, never inspected during development). Every report-grade
number below uses \texttt{REPORT\_SEEDS} (spec \S23.6's $\ge$20-seed
minimum), never \texttt{DEV\_SEEDS}.

\section{Baselines and Ablations Implemented}

\begin{table}[h]
\centering
\small
\begin{tabular}{lp{9.5cm}}
\hline
ID & Description \\
\hline
B1 Static Plan & Never replan (spec \S22) \\
B2 Full Reoptimization & Re-solve the whole remaining problem on every event; pluggable solver (greedy or real CRP\_RL) \\
B3 Periodic Replanning & Re-solve every $k$-th event, otherwise keep the current plan \\
B4 Event-triggered, No Stability & Same trigger + freeze horizon as SAR-CRP, but $\lambda=\mu=0$ (objective is operational cost only) \\
B5 MPC Receding Horizon & Freeze a fixed-size prefix, unconditionally re-solve the tail against the post-frozen-prefix state every event \\
B6 SAR-CRP Core & Proposed method: trigger + freeze horizon + local-search repair + stability-aware objective \\
\hline
A1 No Trigger & Always replan (removes the impact threshold) \\
A2 No Freeze Horizon & Allow the repair to touch the entire plan \\
A3 No Stability Cost & Optimize operational cost only ($\lambda=0$) \\
A4 No Local Search & Minimal feasibility repair or full reoptimization only \\
A5 No Data Confidence Penalty & Ignore state confidence in the objective \\
A6 No Blocking Impact & Impact estimate excludes $I_{blocking}$ \\
\hline
\end{tabular}
\caption{B1--B6 (spec \S22, \S40) and A1--A6 (spec \S25), as implemented in \texttt{sarcrp.baselines} and \texttt{sarcrp.ablations}.}
\end{table}

\section{Experiment 1: Main Factorial Results}

Factorial design (spec \S23): uncertainty level $\times$ freeze size
$\times$ $\lambda$ $=$ 3 $\times$ 3 $\times$ 3, all 6 methods (B1--B6),
20 seeds (\texttt{REPORT\_SEEDS}) $=$ 3240 episodes total, on
\texttt{small\_layout\_mvp.json}. Run via \texttt{run\_experiment1.py}
(commit \texttt{2617a50}, duration 481.1s, logged in
\texttt{experiments/logs/run\_log.jsonl}).

\textbf{Correctness note.} Two real bugs were caught and fixed against
this experiment's own output before treating any number below as final:
(1) the significance-test helper collapsed the full 27-cell factorial grid
into one seed-keyed dictionary without filtering first, so a duplicate
seed silently kept whichever grid cell was iterated last
(\texttt{freeze\_size=5}, $\lambda=1.0$) instead of any deliberate
operating point; and (2) the MPC baseline (B5) solved its tail against the
\emph{original, untouched} yard state, so its own frozen prefix's moves
were silently re-planned again by the tail, making \texttt{is\_plan\_valid}
reject nearly every episode and the $M_{\mathrm{inf}}=10^6$ invalidity
penalty dominate its reported cost. Both are fixed (Wilcoxon now filters
to one explicit \texttt{(uncertainty\_level, freeze\_size=3, $\lambda$=1.0)}
cell --- spec \S48's own SAR-CRP defaults --- per uncertainty level; MPC now
shadow-applies its frozen prefix before solving the tail). This experiment
was re-run after the MPC fix; the numbers below are from that re-run.

\begin{table}[h]
\centering
\small
\begin{tabular}{llrrrr}
\hline
Uncertainty & Method & Total cost & Op.\ cost & Changed actions & Fallback rate \\
            &        & (mean)     & (mean)    & (mean)           &               \\
\hline
low    & Static                        & 7.042  & 7.042 & 0.00  & 1.000 \\
low    & Full Reoptimization           & 19.045 & 6.700 & 59.10 & 0.000 \\
low    & Periodic                      & 11.118 & 6.991 & 18.05 & 0.851 \\
low    & Event-triggered, No Stability & 7.042  & 7.042 & 0.00  & 1.000 \\
low    & MPC Receding Horizon          & 16.632 & 7.547 & 49.45 & 0.000 \\
low    & SAR-CRP                       & 7.042  & 7.042 & 0.00  & 1.000 \\
\hline
medium & Static                        & 7.052  & 7.052 & 0.00  & 1.000 \\
medium & Full Reoptimization           & 21.866 & 6.612 & 71.50 & 0.000 \\
medium & Periodic                      & 11.644 & 6.959 & 21.35 & 0.850 \\
medium & Event-triggered, No Stability & 7.052  & 7.052 & 0.00  & 1.000 \\
medium & MPC Receding Horizon          & 17.789 & 7.560 & 54.10 & 0.000 \\
medium & SAR-CRP                       & 7.052  & 7.052 & 0.00  & 1.000 \\
\hline
high   & Static                        & 7.059  & 7.059 & 0.00  & 1.000 \\
high   & Full Reoptimization           & 23.333 & 6.523 & 79.45 & 0.000 \\
high   & Periodic                      & 11.612 & 6.781 & 21.35 & 0.850 \\
high   & Event-triggered, No Stability & 7.059  & 7.059 & 0.00  & 1.000 \\
high   & MPC Receding Horizon          & 18.888 & 7.747 & 59.00 & 0.000 \\
high   & SAR-CRP                       & 7.059  & 7.059 & 0.00  & 1.000 \\
\hline
\end{tabular}
\caption{Experiment 1: mean over freeze\_size $\times$ $\lambda$ $\times$ 20 seeds ($n=180$ per row). Source: \texttt{experiments/results/experiment1\_results.csv}, post MPC fix (commit 2617a50).}
\end{table}

\textbf{SAR-CRP vs.\ each baseline (spec \S23.6 protocol: paired Wilcoxon
signed-rank, \texttt{zero\_method="pratt"}, Holm--Bonferroni correction
across the 5 comparisons, Cliff's delta), at the default operating point
(\texttt{freeze\_size=3}, $\lambda=1.0$), per uncertainty level:}

\begin{table}[h]
\centering
\small
\begin{tabular}{llrrlr}
\hline
Uncertainty & Baseline & $p$-value & $n_{\text{nonzero}}/n$ & Holm-sig.\ & Cliff's $\delta$ \\
\hline
low    & Static                        & 1.0000 & 0/20  & No  & 0.000 \\
low    & Full Reoptimization           & 0.0000 & 20/20 & Yes & $-$1.000 \\
low    & Periodic                      & 0.0000 & 20/20 & Yes & $-$1.000 \\
low    & Event-triggered, No Stability & 1.0000 & 0/20  & No  & 0.000 \\
low    & MPC Receding Horizon          & 0.0000 & 20/20 & Yes & $-$1.000 \\
\hline
medium & Static                        & 1.0000 & 0/20  & No  & 0.000 \\
medium & Full Reoptimization           & 0.0000 & 20/20 & Yes & $-$1.000 \\
medium & Periodic                      & 0.0000 & 20/20 & Yes & $-$1.000 \\
medium & Event-triggered, No Stability & 1.0000 & 0/20  & No  & 0.000 \\
medium & MPC Receding Horizon          & 0.0000 & 20/20 & Yes & $-$1.000 \\
\hline
high   & Static                        & 1.0000 & 0/20  & No  & 0.000 \\
high   & Full Reoptimization           & 0.0000 & 20/20 & Yes & $-$1.000 \\
high   & Periodic                      & 0.0000 & 20/20 & Yes & $-$1.000 \\
high   & Event-triggered, No Stability & 1.0000 & 0/20  & No  & 0.000 \\
high   & MPC Receding Horizon          & 0.0000 & 20/20 & Yes & $-$1.000 \\
\hline
\end{tabular}
\caption{Source: \texttt{experiments/results/experiment1\_significance.csv}, post MPC fix (commit 2617a50).}
\end{table}

\textbf{SAR-CRP ties Static and B4 exactly (0/20 nonzero pairs) at every
uncertainty level.} This is the same real, explainable result the MVP
report already documented, not a new bug: spec \S11.2's
$\mathit{RetrievalDelay}(P)$ term only sums position over \emph{urgent}
containers, and only \texttt{URGENT\_INSERTION} events populate the urgent
set; a pure \texttt{ORDER\_SWAP}/ETA/\texttt{PROBABILITY\_UPDATE} event
changes the queue but changes no plan's operational cost, so there is
nothing for repair to improve on. \S20 SC4 (below) confirms this
instance's mean event impact (0.090) sits well under
$\theta_{\text{impact}}=0.30$, so the trigger essentially never fires on
this benchmark regardless of \texttt{freeze\_size}/$\lambda$ -- consistent
with Task 27/28's own finding that varying those two factors produced no
observable behavioral difference here. SAR-CRP beats Full Reoptimization,
Periodic, and MPC with $p<0.0001$ (Holm-significant) and Cliff's
$\delta=-1.0$ at every uncertainty level: those three baselines all incur
real reoptimization/instability cost every event, while SAR-CRP correctly
recognizes (via the trigger) that repairing is not worth it on this
benchmark and defaults to Static's behavior instead.

\section{Experiment 3: Cross-Layout}

Layout A (\texttt{small\_layout\_mvp.json}, 10 containers, timeout 1.0s),
Layout B (\texttt{layout\_b.json}, 15 containers, timeout 5.0s), Layout C
(\texttt{layout\_c.json}, 24 containers, timeout 30.0s) --- spec \S17's
own size-based timeout tiers, same hyperparameters (spec \S48 defaults)
otherwise, no per-layout tuning. 20 seeds (\texttt{REPORT\_SEEDS}), 3
methods (Static, Full Reoptimization, SAR-CRP).

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\hline
Method & Performance drop, Layout B vs.\ A & Performance drop, Layout C vs.\ A \\
\hline
Static & +141.5\% & +325.6\% \\
Full Reoptimization & +43.6\% & +103.2\% \\
SAR-CRP & +141.5\% & +325.6\% \\
\hline
\end{tabular}
\caption{Relative total-cost change vs.\ Layout A (spec \S24.5/\S50). Source: \texttt{experiments/results/cross\_layout\_results.csv}.}
\end{table}

SAR-CRP's performance drop tracks Static's exactly on both larger layouts,
for the same reason as Experiment 1: it never crosses the impact trigger
threshold on any of the three layouts at these defaults, so it always
falls back to Static's plan. Full Reoptimization's smaller relative drop
reflects that its absolute total cost already starts much higher on
Layout A (it pays a large stability cost every event regardless of
layout size), so the same absolute cost increase on larger layouts is a
proportionally smaller jump.

\section{Experiment 4: Data Confidence Sensitivity}

Confidence pinned per event (spec \S23's own protocol for isolating
confidence's effect from severity) at 1.0/0.7/0.4/0.2, 20 seeds
(\texttt{REPORT\_SEEDS}), on \texttt{small\_layout\_mvp.json}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrr}
\hline
Method & \multicolumn{4}{c}{Total cost mean, by fixed confidence} \\
       & 1.0 & 0.7 & 0.4 & 0.2 \\
\hline
Static & 7.049 & 7.049 & 7.049 & 7.049 \\
Full Reoptimization & 20.393 & 21.493 & 22.593 & 23.326 \\
SAR-CRP & 7.049 & 7.049 & 7.049 & 7.049 \\
\hline
\end{tabular}
\caption{Source: \texttt{experiments/results/experiment4\_results.csv}.}
\end{table}

Full Reoptimization's total cost rises monotonically as confidence drops
(20.39 $\to$ 23.33, spec \S11.4's data-confidence penalty scaling with how
much the relied-upon plan information has changed) --- the mechanism spec
\S23 Experiment 4 is meant to exercise works as intended. Static and
SAR-CRP show no confidence sensitivity on this instance for the same
reason as Experiments 1 and 3: both never change their plan
(\texttt{changed\_actions\_total}=0 in every row), so
\texttt{data\_confidence\_cost} --- which is computed from the delta
between the new and old plan --- is trivially zero regardless of the
confidence level fed in.

\section{Sanity Checks (spec \S20, \S49)}

\begin{itemize}
  \item SC1 (not too easy): \textbf{PASS} --- the base plan requires real relocations (bottom-first round-robin retrieval order).
  \item SC2 (not too hard): \textbf{PASS} --- measured on Full Reoptimization's incomplete-plan rate, not Static's trivial fallback rate.
  \item SC3 event-type frequency: \texttt{URGENT\_INSERTION}=0.233, \texttt{ORDER\_SWAP}=0.400, \texttt{PROBABILITY\_UPDATE}=0.144, \texttt{ETA\_LATE}=0.111, \texttt{ETA\_EARLY}=0.089, \texttt{STALE\_INFORMATION}=0.022.
  \item SC4 (mean impact in $[0.2, 0.8]$): \textbf{FAIL} (mean=0.090). This is the direct explanation for Experiments 1/3/4's SAR-CRP-ties-Static finding above: this benchmark instance's synthetic event stream produces impact scores well below $\theta_{\text{impact}}=0.30$ on average, so the trigger rarely fires. Flagged here rather than hidden --- see Limitations.
\end{itemize}

\section{Ground Truth}

\textbf{Small, exhaustive (spec \S21.1).} On \texttt{tiny\_ground\_truth.json}
(3 stacks $\times$ 2 containers, 6 total, branch-and-bound exhaustive
search, no randomness so no seed sweep needed): optimal $=4$ relocations,
greedy $=5$ relocations, optimality gap $=25.0\%$.

\textbf{Offline proxy, larger layouts (spec \S21.2).} Full Reoptimization
run with a 300s extended timeout as an offline high-quality
\emph{proxy} for the optimum (never called the true optimum, per spec
\S21.2's explicit wording) on Layouts B and C: gap vs.\ proxy $=0.0\%$ on
both (17 vs.\ 17 relocations on Layout B, 30 vs.\ 30 on Layout C) --- expected,
since the greedy solver is non-iterative and does not improve with more
wall-clock time.

\section{Metrics Completeness (spec \S24, Task 30)}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrr}
\hline
Method & Invalid rate & Timeout rate & Runtime P95 (s) & Stability cost (mean) \\
\hline
Static & 0.0000 & 0.0000 & 0.00000 & 0.0000 \\
Full Reoptimization & 0.0000 & 0.0000 & 0.00009 & 14.1735 \\
SAR-CRP & 0.0000 & 0.0000 & 0.26330 & 0.0000 \\
MPC (post-fix) & 0.0000 & 0.0000 & 0.00015 & 9.3986 \\
\hline
\end{tabular}
\caption{\texttt{small\_layout\_mvp.json}, \texttt{REPORT\_SEEDS} (20 seeds).}
\end{table}

\texttt{invalid\_rate} was 0.0 for every method \emph{after} the MPC fix
above --- before that fix, MPC alone showed \texttt{invalid\_rate}=1.0 on
a 3-seed spot check, which is exactly how the bug was caught: this is the
first time \texttt{is\_plan\_valid}/$M_{\mathrm{inf}}$ has actually been
exercised as a live check in this codebase rather than dead code (every
call site had passed \texttt{is\_valid=True} unconditionally through Task
6--29). SAR-CRP's own \texttt{runtime\_p95\_sec} (0.263s) is noticeably
higher than Full Reoptimization's (0.00009s) despite SAR-CRP mostly
falling back to KEEP on this instance --- its impact estimation and
trigger check still run on every event even when the outer decision is
KEEP.

\section{Real Solver Comparison (Task 33/34, spec \S43)}
\label{sec:real-solver}

\texttt{sarcrp.crp\_rl\_adapter} wraps the real trained CRP\_RL model
(Shin et al.\ 2026, MIT-licensed code) behind the \texttt{solve\_crp}
interface, reusing only its relocation \emph{decisions} -- the resulting
plan is scored by this project's own \texttt{objective.py}, not CRP\_RL's
own travel-time cost model.

\textbf{Out-of-distribution sanity check only (not evidence of relative
quality).} \texttt{small\_layout\_mvp.json} has 10 containers, far below
CRP\_RL's training distribution (35--70 containers per its own
\texttt{baselines/test.py}). Greedy total\_cost\_mean over seeds 0--4:
[15.62, 19.45, 20.45, 17.13, 25.51]; CRP\_RL: [28.95, 38.65, 44.54, 29.11,
34.81]. Greedy is cheaper here, but the comparison is out-of-distribution
and must not be read as evidence about CRP\_RL's true quality.

\textbf{In-distribution comparison (the one comparison usable as
evidence).} A dedicated 50-container instance
(\texttt{crp\_rl\_scale\_instance.json}, 10 stacks $\times$ 5 containers,
bottom-first round-robin, inside CRP\_RL's [35, 70]-container trained
range) was generated for this purpose. Greedy total\_cost\_mean over
seeds 0--4: [78.30, 84.16, 63.29, 75.86, 75.40] (mean $\approx$75.4);
CRP\_RL: [155.17, 151.17, 177.74, 142.64, 155.84] (mean $\approx$156.5).
Greedy still outperforms the real trained model by a wide margin
($\approx$48\% lower mean total cost) even within CRP\_RL's own trained
size range. \textbf{Caveat:} because the adapter only reuses CRP\_RL's
relocation decisions and rescoring happens under SAR-CRP's own objective
(not the metric CRP\_RL was trained to minimize), this is evidence that
CRP\_RL's relocation choices do not beat the greedy heuristic \emph{under
this objective}, not a general claim that CRP\_RL is a worse CRP solver
in an absolute sense.

\section{Reproducibility}

Every run cited above is recorded in
\texttt{experiments/logs/run\_log.jsonl} (git commit, hostname, exact
params, wall-clock duration, output paths) -- gitignored locally, one JSON
line per invocation. Key entries: \texttt{run\_cross\_layout.py}
(13.26s), \texttt{run\_experiment4.py} (41.79s),
\texttt{run\_experiment1.py} (481.06s, re-run after the MPC
fix, git commit \texttt{2617a50}).

\section{Limitations}

\begin{itemize}
  \item \textbf{Experiment 5} (operator-acceptance study) is intentionally
    omitted, per spec \S23's own framing of it as optional -- noted here
    explicitly rather than silently dropped.
  \item \textbf{This benchmark instance rarely triggers SAR-CRP's
    repair path.} SC4 (mean impact 0.090 vs.\ $\theta_{\text{impact}}=0.30$)
    is the root cause behind Experiments 1/3/4 all showing SAR-CRP tied to
    Static: the synthetic event generator on this instance's parameters
    essentially never produces an event severe enough to cross the
    trigger. The comparisons against Full Reoptimization/Periodic/MPC are
    still informative (they show SAR-CRP correctly avoids unnecessary
    churn), but this suite does not yet demonstrate a scenario where
    SAR-CRP's repair mechanism produces a strict, active win over Static.
    A benchmark instance/event-parameter combination that reliably crosses
    the trigger is left to future work.
  \item \textbf{CRP\_RL comparison caveat} as stated in \S\ref{sec:real-solver} above:
    rescoring under SAR-CRP's own objective, not CRP\_RL's native metric.
  \item \textbf{Base-paper citations} (Shin et al.\ 2026, Zhou \& Zhang
    2024, Zhang et al.\ 2025) were verified for DOI/venue in an earlier
    phase of this project but are not re-verified in this report.
\end{itemize}

\section{Conclusion}

The full baseline suite (B1--B6), all six ablations (A1--A6), the
statistical protocol (spec \S23.6), cross-layout generalization, ground
truth/offline-proxy comparisons, and a real trained-model comparison are
now all implemented and exercised with real, seeded runs rather than
placeholders. Two genuine implementation bugs were caught and fixed
directly from this experiment suite's own output before being reported
(the significance-test grid-cell aggregation bug, and the MPC tail-state
bug) -- both are documented above rather than silently corrected. The
main open finding is that this specific benchmark instance's default
event parameters keep SAR-CRP's trigger from firing in practice; the
suite is otherwise ready to be pointed at a higher-impact benchmark
instance as the natural next step.

\end{document}
